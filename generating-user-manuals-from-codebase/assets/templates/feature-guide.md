<!-- 模板示例为中文；输出语言不同时连同节标题、警告标签一起翻译。<> 内为占位说明，替换后删除。 -->

# `<用户目标，动词开头，例：导出订单>`

`<一段话：这个功能帮你完成什么、什么时候会用到。>`

## 开始之前

- `<已验证的权限：例：需要「订单管理」权限；没有该权限时看不到 **导出** 按钮。>`
- `<已验证的前置条件或所需材料>`

## `<完成主要任务，例：导出选中的订单>`

1. `<动作，使用界面上的实际文字，加粗按钮 / 菜单名。例：在左侧菜单点击 **订单**。>`
2. `<动作。例：勾选要导出的订单，然后点击列表右上角的 **导出**。>`
   ![`<alt：界面状态 + 被标注的元素，≤155 字，不含"截图"二字>`](`<../../assets/<feature-id>/01-<step-slug>.png>`)
3. `<在对话框里的选择 / 输入。多个顺序动作时用编号角标图。>`
   ![`<alt>`](`<../../assets/<feature-id>/02-<step-slug>.png>`)

> **注意：** `<紧贴不可逆 / 有后果的动作之前写后果。例：删除后无法恢复，关联的退款记录也会一并删除。>`

4. `<确认或提交。例：点击 **确认导出**。>`

**结果：** `<可观察的成功信号与下一步。例：右上角显示「导出任务已创建」，文件生成后会出现在 **下载中心**。>`

## `<变体或后续任务，例：导出全部订单>`

`<仅当有证据且有用时保留。>`

## 出现问题时

- **`<可观察的现象，例：看不到 导出 按钮>`：** `<已验证的恢复动作或解释，例：你的账号没有「订单管理」权限，请联系管理员开通。>`
- **`<现象>`：** `<恢复动作>`

---

<!-- 以下为编辑备注，不属于用户正文；阶段 5 汇总到 _editor-notes.md 后可删除 -->

## 发布前待确认

- `<缺失的事实与它为什么重要。例：导出文件的保留期未在代码中找到，正文暂未提及。>`
